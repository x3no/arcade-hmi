# Core dependencies
# On Raspberry Pi / DietPi, install via apt instead of pip to avoid compilation:
#   sudo apt-get install python3-pygame python3-rpi.gpio
pygame>=2.0.0

# Raspberry Pi specific (only needed on Pi)
RPi.GPIO>=0.7.0; platform_machine == "aarch64" or platform_machine == "armv7l"
